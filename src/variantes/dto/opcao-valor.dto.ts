import { IsNotEmpty, isNumber, IsNumber, IsOptional, IsString } from "class-validator";

export class OpcaoValor {
    
    opc_id: number;
    opc_nome: string;
    opc_valor: string;
            
            

}
